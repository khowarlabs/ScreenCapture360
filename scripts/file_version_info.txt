# UTF-8
#
# For more details about fixed file info 'flags' see:
# https://docs.microsoft.com/en-us/windows/win32/api/verrsrc/ns-verrsrc-tagvs_fixedfileinfo
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items.
    # can be partitioned using dots at runtime, but here commas are used.
    filevers=(0, 1, 0, 0),
    prodvers=(0, 1, 0, 0),
    # Contains a bitmask that specifies the valid bits 'flags'r
    mask=0x3f,
    # Contains a bitmask that specifies the attributes of the file.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and Windows
    OS=0x40004,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The specific type of file.
    # 0x0 - the file is unknown to the Windows subsystem.
    subtype=0x0,
    # Specifies the date and time the file was created.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        '040904B0',
        [StringStruct('CompanyName', 'KhowarLabs'),
        StringStruct('FileDescription', 'PyQt6 Screen Recorder'),
        StringStruct('FileVersion', '0.1.0'),
        StringStruct('InternalName', 'Screen Recorder'),
        StringStruct('LegalCopyright', 'Copyright (c) 2026 KhowarLabs'),
        StringStruct('OriginalFilename', 'Screen Recorder.exe'),
        StringStruct('ProductName', 'Screen Recorder'),
        StringStruct('ProductVersion', '0.1.0')])
      ]), 
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
